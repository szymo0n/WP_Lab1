#include <stdio.h>
#include <stdbool.h>

int main() {
    char temp;
    int °C , °F;
    scanf(%d ,&°C or &°F);
    if(°C= (°F − 32) / 1.8);
    if(°F = °C × 1.8 + 32);
    printf("Zmienna '°C' ma wartość: %d" °C, sizeof(°C));
    printf("Zmienna '°F' ma wartość: %d" °F, sizeof(°F));
    else( °F or °C = false);
return 0;
}